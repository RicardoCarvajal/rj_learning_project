//Los siguientes valores en un condicional son falsos para javascript

Boolean(false);
Boolean(0);
Boolean(undefined);
Boolean(null);
Boolean(NaN);
Boolean("");

// Cabe destacar que todo numero que no sea 0, asi sea negativo es verdadero

// Esto nos puede ayudar a realizar logicas condicionales con menos lineas de codigo 